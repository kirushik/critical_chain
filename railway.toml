# Railway deployment configuration using Railpack
# This ensures assets are precompiled during the build phase

[build]
builder = "RAILPACK"
buildCommand = "bundle exec rails assets:precompile"

[deploy]
startCommand = "bundle exec puma -C config/puma.rb"
# Railway will automatically set RAILS_ENV=production
