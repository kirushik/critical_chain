<%# Replace the title section %>
<%= turbo_stream.replace "estimation_title" do %>
  <div id="estimation_title">
    <h1><%= @estimation.decorate.editable :title %></h1>
  </div>
<% end %>

<%# Replace the mode toggle (handles tracking_mode updates) %>
<%= turbo_stream.replace "mode_toggle" do %>
  <div id="mode_toggle">
    <%= render 'mode_toggle' %>
  </div>
<% end %>
