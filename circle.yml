deployment:
  production:
    branch: master
      - heroku pgbackups:capture
      - git push git@heroku.com:critical-chain.git $CIRCLE_SHA1:refs/heads/master
      - heroku run rake db:migrate --app critical-chain
