# Nixpacks configuration for Railway deployment
# Ensures proper asset precompilation for Rails with Propshaft

[phases.build]
cmds = [
  "bundle exec rails assets:precompile"
]

[start]
cmd = "bundle exec puma -C config/puma.rb"
